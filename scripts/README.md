# Scripts Directory

This repository is currently notebook-first.

This directory is reserved for optional helper scripts such as:

- notebook export helpers
- plotting utilities
- postprocessing tools
- dataset or checkpoint conversion scripts

It is intentionally minimal at the moment.
